#!/usr/bin/env ruby

begin
  require 'testgen'
rescue LoadError
  $LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))
  require 'testgen'
end

tg = TestDataGenerator.new
tg.eval_script(ARGF.read)
tg.print_inserts
