#!/usr/bin/env ruby

begin
  require 'testgen'
rescue LoadError
  $LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))
  require 'testgen'
end

TestDataGenerator.new("unused dsn").generate_data(ARGF.read)
