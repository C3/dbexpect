#!/usr/bin/env ruby

begin
  require 'testgen'
rescue LoadError
  $LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))
  require 'testgen'
end

tg = TestGen.new
tg.great_expectations(ARGV[0],ARGF.read)
