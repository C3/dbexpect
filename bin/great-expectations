#!/usr/bin/env ruby

begin
  require 'testgen'
rescue LoadError
  $LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))
  require 'testgen'
end

target_db = TargetDatabase.from_dsn(ARGV.shift)
exit(TestDataGenerator.new().great_expectations(ARGF.read,target_db))
