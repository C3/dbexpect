#!/usr/bin/env ruby

begin
  require 'testgen'
rescue LoadError
  $LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))
  require 'testgen'
end
db = Database.from_dsn(ARGV.shift)
exit(TestDataGenerator.new().setup_test(ARGF.read,db))
